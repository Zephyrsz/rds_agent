# RDS Agent 集成方式分析报告

## 1. 当前项目集成方式总览

### 1.1 架构概述

RDS Agent 采用了**分层集成架构**，核心是基于 SQLite metadata + LangGraph workflow 的查询引擎，对外提供多种集成接口：

```
┌─────────────────────────────────────────────────────────────┐
│                    外部集成层                                  │
├────────────┬──────────────┬──────────────┬──────────────────┤
│ Python SDK │ MCP Server   │  REST API    │  Function Calling │
│            │  (stdio)     │  (HTTP)      │  LangChain Tool   │
└────────────┴──────────────┴──────────────┴──────────────────┘
                              │
                    ┌─────────┴──────────┐
                    │  RDSAgent (SDK核心)  │
                    └─────────┬──────────┘
                              │
                    ┌─────────┴──────────┐
                    │ LangGraph Workflow │
                    └─────────┬──────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────┴────────┐  ┌────────┴────────┐  ┌────────┴────────┐
│ SQLiteCatalog  │  │ SQLiteSemantic  │  │    Compiler     │
│  (Schema)      │  │    (Metrics)    │  │   (SQL Gen)     │
└────────────────┘  └─────────────────┘  └─────────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                    ┌─────────┴──────────┐
                    │   SQLGuard         │
                    │   DuckDB Executor  │
                    └────────────────────┘
```

### 1.2 当前集成方式详细分析

#### 1.2.1 Python SDK（`integration/sdk.py`）- **推荐主路径**

**特点：**
- 进程内集成，无网络开销
- 性能最优（~100ms）
- 完整使用 SQLite metadata runtime
- 支持同步和异步接口
- 提供上下文管理器

**适用场景：**
- 同一 Python 进程内的 Agent
- 需要最佳性能的场景
- 需要共享上下文的场景

**核心代码模式：**
```python
with RDSAgent(
    config_dir="config",
    db_path="business.duckdb",
    metadata_db_path="metadata.db"  # 持久化 SQLite metadata
) as agent:
    result = agent.query("问题")
```

**优势：**
- ✅ 唯一完全使用 SQLite metadata 的主路径
- ✅ 支持 metadata_db_path 参数，可跨会话复用
- ✅ 自动 YAML → SQLite 迁移
- ✅ 完整错误处理和结果验证

#### 1.2.2 MCP Server（`integration/mcp_server.py`）- **推荐主路径**

**特点：**
- 基于 MCP stdio 协议
- 只暴露单一粗粒度工具：`query_data(question)`
- 内部委托给 SDK
- 支持 DeepSeek Harness 等 MCP 客户端

**适用场景：**
- DeepSeek Harness 集成
- 支持 MCP 的 Agent 框架
- 需要进程隔离的场景

**DeepSeek Harness 集成配置：**
```yaml
# deepseek-harness.cordis.yml
- insert:
    - id: rds-agent-mcp
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        serverName: rds
        transport: stdio
        command: ./venv/bin/python
        args: [-m, integration.mcp_server]
        cwd: !!js process.env.RDS_AGENT_ROOT
        env:
          OPENAI_API_KEY: !!js process.env.RDS_LLM_API_KEY
          RDS_DB_PATH: !!js process.env.RDS_DB_PATH || ':memory:'
```

**优势：**
- ✅ 完全基于 SDK，共享 SQLite metadata 路径
- ✅ 标准 MCP 协议，兼容性好
- ✅ 进程隔离，稳定性高

**当前限制：**
- ⚠️ CLI 未暴露 `metadata_db_path` 参数
- 需要自定义启动代码才能复用持久化 SQLite metadata

#### 1.2.3 REST API（`integration/rest_api.py`）- **待统一路径**

**特点：**
- FastAPI 实现
- 提供 HTTP 接口
- 支持异步处理
- 自动生成 Swagger 文档

**适用场景：**
- 跨语言/跨平台集成
- 微服务架构
- 需要 HTTP 协议的场景

**当前状态：**
- ⚠️ **仍直接使用 YAML Catalog/SemanticLayer**
- ⚠️ 未集成 SDK/SQLite 路径
- ⚠️ 认证、CORS 仍是开发态配置
- ⚠️ dataclass Schema 序列化有兼容问题

**需要改进：**
```python
# 当前（YAML 路径）
catalog = DatabaseCatalog(config_dir)
semantic_layer = SemanticLayer(config_dir)

# 应改为（SDK 路径）
agent = RDSAgent(
    config_dir=config_dir,
    db_path=db_path,
    metadata_db_path=metadata_db_path
)
# 然后通过 agent.query() 委托
```

#### 1.2.4 Function Calling（`integration/function_calling.py`）- **待统一路径**

**特点：**
- 符合 OpenAI/Anthropic Function Calling 规范
- 定义三个函数：`query_database`、`get_database_schema`、`get_available_metrics`
- LLM 原生支持

**适用场景：**
- OpenAI GPT-4/Claude Agent
- 需要 LLM 自主决策工具调用
- 支持并行函数调用

**当前状态：**
- ⚠️ **仍直接使用 YAML 初始化**
- ⚠️ 未集成 SDK/SQLite 路径
- ⚠️ Schema 序列化有 dataclass/字典兼容问题

**需要改进：**
应该封装 SDK 并复用其 SQLite metadata 管理

#### 1.2.5 LangChain Tool（`integration/langchain_tool.py`）- **待统一路径**

**特点：**
- 封装为 LangChain BaseTool
- 提供 `rds_query` 和 `rds_schema` 两个工具
- 支持同步和异步

**适用场景：**
- 基于 LangChain 的 Agent
- 需要工具链组合的场景

**当前状态：**
- ⚠️ **仍直接使用 YAML 初始化**
- ⚠️ 类名大小写不一致（`RDSSchemaTool` vs `RDSSchematool`）
- ⚠️ 未集成 SDK/SQLite 路径

---

## 2. DeepSeek Harness 集成方式分析

### 2.1 DeepSeek Harness 架构

DeepSeek Harness 是一个**插件化的 Agent 运行时**，所有组件都是插件：

```
┌─────────────────────────────────────────────┐
│         DeepSeek Harness Runtime            │
├──────────────┬──────────────┬───────────────┤
│ Model Adapter│ Tool Registry│  Agent Loop   │
│   (可插拔)    │   (可插拔)    │   (可插拔)     │
└──────────────┴──────────────┴───────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
┌───────┴──────┐ ┌──┴───────┐ ┌─┴────────┐
│ MCP Plugins  │ │ JS Tools │ │ Native   │
│   (stdio)    │ │          │ │ Plugins  │
└──────────────┘ └──────────┘ └──────────┘
```

### 2.2 当前 RDS Agent 的 Harness 集成

**方式：MCP stdio 子进程**

```yaml
# deepseek-harness.cordis.yml - Cordis overlay 配置
- insert:
    - id: rds-agent-mcp
      name: '@deepseek-ai/dsh-mcp-client'  # MCP 客户端插件
      config:
        serverName: rds
        transport: stdio                    # stdio 传输
        command: ./venv/bin/python
        args: [-m, integration.mcp_server]  # 启动 MCP server
        cwd: !!js process.env.RDS_AGENT_ROOT
```

**特点：**
- 使用 Cordis `--patch` 机制注入
- 作为独立子进程运行
- 通过 stdio JSON-RPC 通信
- 工具注册为 `mcp__rds__query_data`

### 2.3 Harness 插件生态的其他集成方式

根据搜索结果，DeepSeek Harness 支持多种插件集成方式：

#### 方式 1：MCP stdio 插件（当前使用）
- **特点：** 标准 MCP 协议，进程隔离
- **示例：** 当前 RDS Agent 的实现
- **优势：** 稳定性高，语言无关
- **劣势：** 有进程通信开销

#### 方式 2：Native Harness 插件
- **特点：** 直接作为 Harness 的 JavaScript/TypeScript 插件
- **实现：** 需要编写符合 Harness 插件规范的代码
- **优势：** 性能最优，无序列化开销
- **劣势：** 需要 JS/TS 实现，或通过 FFI 调用 Python

#### 方式 3：Composio Connect（托管 MCP）
- **特点：** 通过 Composio 平台托管的统一 MCP 端点
- **示例：** GitHub、Discord、Cloudflare 等 1000+ 应用
- **优势：** 无需自己部署，认证由平台处理
- **劣势：** 依赖第三方平台，可能有数据隐私问题

#### 方式 4：MCP Apps（带 UI 的工具）
- **特点：** MCP 工具可携带 `_meta.ui.resourceUri` 元数据
- **示例：** `sugarforever/dsh-mcp-apps`
- **优势：** 支持可视化交互
- **劣势：** 需要额外的 UI 开发

---

## 3. 其他插件式集成方式探索

### 3.1 基于 gRPC 的插件系统

**架构：**
```
┌──────────────────────────────────────┐
│       RDS Agent Core Service         │
│      (gRPC Server + Metadata)        │
└──────────────────────────────────────┘
           │ gRPC Stream
    ┌──────┼──────┬──────┬──────┐
    │             │      │      │
┌───┴───┐   ┌────┴───┐ ┌┴───┐ ┌┴──────┐
│Python │   │ Node.js│ │ Go │ │ Rust  │
│Client │   │ Client │ │    │ │ Client│
└───────┘   └────────┘ └────┘ └───────┘
```

**优势：**
- ✅ 性能优于 HTTP REST（二进制协议 + HTTP/2）
- ✅ 强类型，自动生成多语言客户端
- ✅ 支持流式查询（stream）
- ✅ 内置负载均衡和服务发现

**劣势：**
- ❌ 实现复杂度高于 REST
- ❌ 调试不如 HTTP 直观
- ❌ 需要 Protocol Buffers 定义

**适用场景：**
- 微服务架构
- 需要高性能 RPC
- 多语言客户端

**实现要点：**
```protobuf
// rds_agent.proto
service RDSAgent {
  rpc Query(QueryRequest) returns (QueryResponse);
  rpc StreamQuery(QueryRequest) returns (stream QueryChunk);
  rpc GetSchema(SchemaRequest) returns (SchemaResponse);
  rpc GetMetrics(Empty) returns (MetricsResponse);
}
```

### 3.2 基于 WebSocket 的实时插件

**架构：**
```
┌──────────────────────────────────────┐
│     RDS Agent WebSocket Server       │
└──────────────────────────────────────┘
           │ WS Connection
    ┌──────┼──────┬──────┬──────┐
    │             │      │      │
┌───┴───┐   ┌────┴───┐ ┌┴────┐ ┌┴──────┐
│Browser│   │ Mobile │ │ CLI │ │Desktop│
│  App  │   │  App   │ │Tool │ │  App  │
└───────┘   └────────┘ └─────┘ └───────┘
```

**优势：**
- ✅ 全双工通信，支持服务器推送
- ✅ 适合长时间运行的查询
- ✅ 支持查询进度通知
- ✅ 浏览器原生支持

**劣势：**
- ❌ 连接管理复杂
- ❌ 需要处理断线重连
- ❌ 不适合简单的请求-响应模式

**适用场景：**
- 实时数据分析仪表板
- 需要进度反馈的长查询
- 多用户协作场景

**实现要点：**
```python
# 使用 FastAPI WebSocket
@app.websocket("/ws/query")
async def websocket_query(websocket: WebSocket):
    await websocket.accept()
    while True:
        question = await websocket.receive_text()
        # 流式返回查询进度
        await websocket.send_json({"status": "planning"})
        await websocket.send_json({"status": "executing", "progress": 50})
        await websocket.send_json({"status": "complete", "data": result})
```

### 3.3 基于消息队列的异步插件

**架构：**
```
┌────────────┐    ┌──────────┐    ┌───────────────┐
│  Producer  │───>│  Queue   │───>│  RDS Worker   │
│  (Client)  │    │ (Redis/  │    │   Pool        │
└────────────┘    │ RabbitMQ)│    └───────────────┘
                  └──────────┘           │
                       │                 │
                  ┌────┴────┐           \/
                  │ Result  │<──── Result Store
                  │ Channel │      (Redis/DB)
                  └─────────┘
```

**优势：**
- ✅ 解耦生产者和消费者
- ✅ 支持任务队列和优先级
- ✅ 可水平扩展 Worker
- ✅ 支持失败重试
- ✅ 适合批量查询

**劣势：**
- ❌ 架构复杂，引入额外组件
- ❌ 增加延迟
- ❌ 需要管理任务状态

**适用场景：**
- 批量数据处理
- 需要任务调度
- 高并发场景

**实现要点：**
```python
# 使用 Celery
@celery.app.task
def rds_query_task(question: str, user_id: str):
    with RDSAgent() as agent:
        result = agent.query(question)
        store_result(user_id, result)
        return result.data

# Client
task = rds_query_task.delay("问题", "user123")
result = task.get(timeout=30)
```

### 3.4 基于插件框架的模块化系统

**架构：**
```
┌───────────────────────────────────────────┐
│         RDS Agent Plugin Host             │
├───────────────────────────────────────────┤
│   Plugin Loader │ Plugin Registry         │
└───────────────────────────────────────────┘
         │
    ┌────┼────┬──────┬──────┬──────┐
    │         │      │      │      │
┌───┴───┐ ┌──┴──┐ ┌─┴───┐ ┌┴────┐ ┌┴──────┐
│ Query │ │Auth │ │Cache│ │ Log │ │Custom │
│Plugin │ │Plugin│ │Plugin│ │Plugin│ │Plugin │
└───────┘ └─────┘ └─────┘ └─────┘ └───────┘
```

**设计模式：** Hook-based Plugin System

**优势：**
- ✅ 高度可扩展
- ✅ 插件热加载/卸载
- ✅ 插件独立开发和测试
- ✅ 社区可贡献插件

**劣势：**
- ❌ 框架设计复杂
- ❌ 插件间依赖管理
- ❌ 版本兼容性问题

**适用场景：**
- 需要第三方扩展的平台
- 多租户定制化需求
- 开源生态建设

**实现要点：**
```python
# Plugin 接口
class RDSPlugin(ABC):
    @abstractmethod
    def on_query_start(self, question: str) -> None: pass
    
    @abstractmethod
    def on_query_complete(self, result: QueryResult) -> QueryResult: pass

# Plugin Host
class PluginHost:
    def __init__(self):
        self.plugins: List[RDSPlugin] = []
    
    def load_plugin(self, plugin_path: str):
        module = importlib.import_module(plugin_path)
        plugin = module.Plugin()
        self.plugins.append(plugin)
    
    def query(self, question: str):
        # 触发 hooks
        for plugin in self.plugins:
            plugin.on_query_start(question)
        
        result = self._do_query(question)
        
        for plugin in self.plugins:
            result = plugin.on_query_complete(result)
        
        return result
```

### 3.5 基于 Sidecar 模式的容器化插件

**架构（Kubernetes）：**
```
┌─────────────────── Pod ──────────────────┐
│                                           │
│  ┌──────────────┐      ┌──────────────┐  │
│  │ Main App     │<────>│ RDS Sidecar  │  │
│  │ Container    │ IPC  │ Container    │  │
│  └──────────────┘      └──────────────┘  │
│         │                       │         │
└─────────┼───────────────────────┼─────────┘
          │                       │
    ┌─────┴─────┐          ┌─────┴─────┐
    │  App Logs │          │ Metadata  │
    │  Volume   │          │  Volume   │
    └───────────┘          └───────────┘
```

**优势：**
- ✅ 完全隔离，互不影响
- ✅ 独立扩展和升级
- ✅ 共享本地卷，性能好
- ✅ 云原生，适合 K8s

**劣势：**
- ❌ 运维复杂度高
- ❌ 资源占用大
- ❌ 需要容器编排平台

**适用场景：**
- 云原生应用
- 微服务架构
- 多租户 SaaS

**实现要点：**
```yaml
# k8s-deployment.yaml
apiVersion: v1
kind: Pod
metadata:
  name: app-with-rds
spec:
  containers:
  - name: main-app
    image: my-app:latest
    volumeMounts:
    - name: shared-socket
      mountPath: /var/run/rds
  
  - name: rds-sidecar
    image: rds-agent:latest
    volumeMounts:
    - name: shared-socket
      mountPath: /var/run/rds
    - name: metadata
      mountPath: /data/metadata
  
  volumes:
  - name: shared-socket
    emptyDir: {}
  - name: metadata
    persistentVolumeClaim:
      claimName: rds-metadata-pvc
```

### 3.6 基于 WASM 的轻量级插件

**架构：**
```
┌────────────────────────────────────┐
│       Host Runtime (任意语言)        │
├────────────────────────────────────┤
│        WASM Runtime (Wasmtime)     │
└────────────────────────────────────┘
           │ WASI Interface
    ┌──────┼──────┬──────┬──────┐
    │             │      │      │
┌───┴───┐   ┌────┴───┐ ┌┴───┐ ┌┴──────┐
│ Query │   │ Filter │ │Auth│ │ Custom│
│ .wasm │   │ .wasm  │ │.wasm│ │ .wasm │
└───────┘   └────────┘ └────┘ └───────┘
```

**优势：**
- ✅ 沙箱隔离，安全性高
- ✅ 轻量级，启动快
- ✅ 跨平台，语言无关
- ✅ 接近原生性能

**劣势：**
- ❌ WASM 生态还不成熟
- ❌ Python 编译到 WASM 仍有限制
- ❌ 调试困难

**适用场景：**
- 需要沙箱隔离的插件
- 边缘计算
- 浏览器端运行

**实现要点：**
```python
# 使用 wasmtime-py
from wasmtime import Store, Module, Instance

store = Store()
module = Module.from_file(store.engine, 'plugin.wasm')
instance = Instance(store, module, [])

# 调用 WASM 导出的函数
query_fn = instance.exports(store)['query']
result = query_fn(store, question_ptr, question_len)
```

---

## 4. 集成方式对比矩阵

| 集成方式 | 性能 | 隔离性 | 跨语言 | 实现复杂度 | 生态成熟度 | 推荐场景 |
|---------|------|--------|--------|-----------|-----------|---------|
| **Python SDK** | ⭐⭐⭐⭐⭐ | ⭐ | ❌ | ⭐ | ⭐⭐⭐⭐⭐ | Python 同进程 |
| **MCP stdio** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ | ⭐⭐ | ⭐⭐⭐⭐ | DeepSeek Harness |
| **REST API** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 微服务架构 |
| **gRPC** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 高性能 RPC |
| **WebSocket** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 实时应用 |
| **消息队列** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 异步批处理 |
| **Plugin Framework** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ❌ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 扩展平台 |
| **Sidecar** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 云原生 |
| **WASM** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ | ⭐⭐ | 沙箱/边缘 |

---

## 5. 推荐的改进方案

### 5.1 短期改进（1-2 周）

#### 优先级 1：统一 REST/Function Calling/LangChain 到 SDK 路径

**目标：** 让所有集成方式都使用 SQLite metadata

**实施步骤：**

1. **REST API 改造：**
```python
# rest_api.py - 修改后
from integration.sdk import RDSAgent

class RDSAgentAPI:
    def __init__(self):
        self.agent = RDSAgent(
            config_dir=config_dir,
            db_path=db_path,
            metadata_db_path=metadata_db_path  # 持久化
        )
    
    @app.post("/api/v1/query")
    async def query(request: QueryRequest):
        result = await asyncio.to_thread(
            self.agent.query,
            request.question,
            user_context=request.context
        )
        return format_response(result)
```

2. **Function Calling 改造：**
```python
# function_calling.py - 修改后
class RDSAgentFunctionCalling:
    def __init__(self, config_dir: Path, db_path: str = ":memory:"):
        # 直接使用 SDK
        self.agent = RDSAgent(
            config_dir=config_dir,
            db_path=db_path,
            metadata_db_path="var/metadata.db"
        )
    
    def call_function(self, function_name: str, arguments: Dict):
        if function_name == "query_database":
            result = self.agent.query(arguments["question"])
            return format_function_result(result)
```

3. **LangChain Tool 改造：**
```python
# langchain_tool.py - 修改后
class RDSQueryTool(BaseTool):
    def __init__(self):
        super().__init__()
        self.agent = RDSAgent()
    
    def _run(self, question: str):
        result = self.agent.query(question)
        return format_tool_output(result)
```

#### 优先级 2：MCP Server 支持持久化 metadata

**目标：** 允许 MCP server 复用 SQLite metadata 文件

**实施步骤：**

```python
# mcp_server.py - 添加参数
def _parse_args():
    parser.add_argument(
        "--metadata-db-path",
        default=os.environ.get("RDS_METADATA_DB_PATH", None)
    )

# deepseek-harness.cordis.yml - 更新配置
env:
  RDS_METADATA_DB_PATH: !!js process.env.RDS_METADATA_DB_PATH || '/tmp/rds_metadata.db'
```

### 5.2 中期扩展（1-2 个月）

#### 方案 1：添加 gRPC 接口（高性能场景）

**适用场景：**
- 微服务间高频调用
- 需要流式查询
- 多语言客户端

**实施要点：**
```protobuf
// rds_agent.proto
service RDSAgent {
  rpc Query(QueryRequest) returns (QueryResponse);
  rpc StreamQuery(QueryRequest) returns (stream QueryChunk);
}
```

#### 方案 2：添加 WebSocket 接口（实时场景）

**适用场景：**
- 实时仪表板
- 长查询进度反馈
- 多用户协作

**实施要点：**
```python
@app.websocket("/ws/query")
async def websocket_query(websocket: WebSocket):
    async for question in websocket.iter_text():
        async for chunk in agent.stream_query(question):
            await websocket.send_json(chunk)
```

### 5.3 长期演进（3-6 个月）

#### 方案 1：构建插件框架

**目标：** 允许第三方扩展功能

**架构：**
- Hook 系统：`on_query_start`、`on_sql_generated`、`on_query_complete`
- Plugin Registry
- Plugin Marketplace

#### 方案 2：云原生部署

**目标：** 支持 Kubernetes 部署

**架构：**
- Helm Charts
- Operator 模式
- Sidecar 注入
- HPA 自动扩展

---

## 6. 总结和建议

### 6.1 当前状态评估

✅ **优势：**
- SDK 和 MCP 路径已完全使用 SQLite metadata
- 多种集成方式满足不同场景
- 架构清晰，分层合理

⚠️ **需要改进：**
- REST/Function Calling/LangChain 仍使用 YAML 路径
- MCP Server CLI 缺少 metadata_db_path 参数
- 缺少高性能和实时场景的支持

### 6.2 推荐优先级

**P0（必须做）：**
1. 统一 REST/Function Calling/LangChain 到 SDK/SQLite 路径
2. MCP Server 支持持久化 metadata

**P1（应该做）：**
3. 添加 gRPC 接口（如有高性能需求）
4. 完善 REST API 的认证和权限

**P2（可以做）：**
5. 添加 WebSocket 支持（如有实时需求）
6. 构建插件框架（如需扩展生态）

### 6.3 具体建议

1. **短期（2 周内）：** 重构现有集成方式，统一到 SDK
2. **中期（1-2 月）：** 根据实际需求添加 gRPC 或 WebSocket
3. **长期（3-6 月）：** 考虑插件化和云原生部署

### 6.4 DeepSeek Harness 集成建议

**当前方式（MCP stdio）已经是最佳实践：**
- ✅ 标准协议，兼容性好
- ✅ 进程隔离，稳定性高
- ✅ 配置灵活，易于部署

**可选优化：**
- 支持持久化 metadata（避免每次启动都迁移）
- 添加更多粗粒度工具（如 `get_metrics`、`get_schema`）
- 考虑实现 Native Harness 插件（如需极致性能）

---

## 参考资源

- [How to Connect MCP Servers to DeepSeek Harness](https://landing.composio.dev/blog/how-to-connect-mcp-servers-to-deepseek-harness)
- [DeepSeek MCP Apps](https://github.com/sugarforever/dsh-mcp-apps)
- [Composio DeepSeek Integration](https://composio.dev/toolkits/github/framework/deepseek)

---

**报告生成时间：** 2026-09-07  
**项目版本：** Phase 0-2 (meta-in-sqlite branch)  
**分析工具：** Claude (Fable 5)
